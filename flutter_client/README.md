# AI Template Agent Flutter Client

Desktop-focused Flutter UI for managing AI-powered document templates and realtime chat.

## Getting Started

```bash
flutter pub get
flutter run -d chrome
```

Set the backend endpoint via `--dart-define API_BASE_URL=http://localhost:8000` during `flutter run`.
